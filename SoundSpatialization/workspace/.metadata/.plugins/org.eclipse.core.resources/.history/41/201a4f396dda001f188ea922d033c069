package ss.cockatoo;

import java.io.*;

import android.os.*;

public class DemoCockatoo {
	
	// read file into array
	File mFile = new File("R.raw.test");
	// get length of audio stored in file
	int musicLength = (int)mFile.length();
	byte[] music = new byte[musicLength];
	BufferedInputStream bis = new BufferedInputStream(new FileInputStream(mFile));
	
	/** Remove the header from wav file and convert to pcm data */
	public byte[] removeHeader(File file) {
	
	}
}
