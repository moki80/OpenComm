package ss.cockatoo;

import java.io.*;

import android.os.*;

public class DemoCockatoo {
	
	// read file into array
	File file = new File("R.raw.test");
	
	BufferedInputStream bis = new BufferedInputStream(new ByteArrayInputStream());
	
	/** Remove the header from wav file and convert to pcm data */
	public byte[] removeHeader(File file) {
	
	}
}
